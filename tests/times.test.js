const times = require("../src/apis/times/index.js");

test("adds 1 * 2 to equal 2", () => {
	expect(times(1, 2)).toBe(2);
});
test("adds 1 * 2 to equal 2", () => {
	expect(times(1, 2)).toBe(2);
});
test("adds 1 * 2 to equal 2", () => {
	expect(times(1, 2)).toBe(2);
});
test("adds 1 * 2 to equal 2", () => {
	expect(times(1, 2)).toBe(2);
});
test("adds 1 * 2 to equal 2", () => {
	expect(times(1, 2)).toBe(2);
});
test("adds 1 * 2 to equal 2", () => {
	expect(times(1, 2)).toBe(2);
});
