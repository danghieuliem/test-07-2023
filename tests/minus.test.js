const minus = require("../src/apis/minus/index.js");

test("adds 1 - 2 to equal -1", () => {
	expect(minus(1, 2)).toBe(-1);
});
test("adds 1 - 2 to equal -1", () => {
	expect(minus(1, 2)).toBe(-1);
});
test("adds 1 - 2 to equal -1", () => {
	expect(minus(1, 2)).toBe(-1);
});
test("adds 1 - 2 to equal -1", () => {
	expect(minus(1, 2)).toBe(-1);
});
test("adds 1 - 2 to equal -1", () => {
	expect(minus(1, 2)).toBe(-1);
});
