name: hello world
on:
  push:
    path:
      - "!README.md"
      - "!docs/**"
      - "**.js"
    branches:
      - "main"
      - "release"
      - "!dev/**"
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODAY=$(date +%d-%m-%y)" >> $GITHUB_ENV
      - run: echo Hello world, today is $TODAY
